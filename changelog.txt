--------
27/06/26
--------
Audio IR output is now scaled to the sine sweep RMS reference, so captured
impulse responses represent the RMS value of the playback sweep rather than
the peak-normalized sweep amplitude.

Qt Test Sweep now shares the audio-only backend path used by sine playback
when the scanner/NFS backend is unavailable, allowing test sweeps to run even
when the scanner is not connected.

Files touched:
src/harmonic_drive_qt/backend.py
src/nfs/audio.py
tests/test_audio_engine.py
tests/test_qt_backend.py

--------
27/06/26
--------
Added SPL calibration meter weighting options for A-weighted, C-weighted and
unweighted readings. The unweighted option supports calibration from an
electrical input reference source, while A/C can be matched to the physical SPL
meter weighting. The SPL help tooltip now includes the supplied weighting curve
image and clearer calibration guidance.

Files touched:
src/harmonic_drive_qt/audio_setup_pane.py
src/nfs/audio.py
images/frequency_weighting.png
tests/test_qt_audio_setup_pane.py

--------
24/06/26
--------
Added a default-on scanner setting to verify the GRBL controller when opening
the serial port. It can be disabled temporarily to distinguish a reconnect
probe/ACK failure from a scanner construction or configuration failure.

Files touched:
src/nfs/grbl_controller.py
src/harmonic_drive/config_editor.py
config.ini
config_default.ini
tests/test_grbl_controller.py

--------
23/06/26
--------
Small UI fixes and usability updates:

Fixed the Home button sometimes remaining orange after successful homing due
to a transient/stale GRBL alarm state.
Disabled HPF inverse-correction controls now use a muted switch.
Added and normalised units for motion and MockDRO settings.
MockSimulatedDRO speed and update-rate fields now appear only when that GRBL
backend is selected.
Replaced advanced-settings switches with standard disclosure
arrows and gave Grid Generator the same expandable heading style as Audio
Setup.

Files touched:
src/harmonic_drive/config_editor.py
src/harmonic_drive/control.py
src/harmonic_drive_qt/control_pane.py
src/harmonic_drive_qt/grid_pane.py
src/harmonic_drive_qt/settings_dialog.py
src/harmonic_drive_qt/styles.py
src/harmonic_drive_qt/icons/disclosure-closed.svg
src/harmonic_drive_qt/icons/disclosure-open.svg
src/harmonic_drive_qt/icons/toggle-disabled-on.svg
tests/test_qt_audio_setup_pane.py
tests/test_qt_grid_pane.py
tests/test_qt_settings_dialog.py

--------
17/06/26
--------
Bug Fix: Changing the GRBL streamer backend in Settings now takes effect on
config reload. Switching Mock/Arduino no longer requires restarting the app.

Files touched:
src/nfs/grbl_controller.py
tests/test_grbl_controller.py

--------
16/06/26
--------
Bug Fix: Sweep settings not saved correctly to _project.json. A shared file was
incorrectly reverted during previous git commit. File re-patched and commited.

Files touched:
src/harmonic_drive/project.py
src/harmonic_drive/audio_setup.py
src/harmonic_drive_qt/main.py
src/harmonic_drive_qt/main_window.py
tests/test_project.py
tests/test_qt_main_window_project.py

--------
15/06/26
--------

Bug fix: selecting a project/session folder no longer creates a default
<ProjectName>_project.json immediately. The project save file is only written
when the user saves, generates grid data, takes measurements, or explicitly
saves SPL calibration.

Tightened project persistence rules. Audio interface settings, channels, sample
rate and output level are global app settings that persist on reload. Sweep
setup is project-tied and saved to project JSON. Sine tone settings always
return to their startup defaults of 1 kHz indefinite duration.

Bug fix: Grid Generator waypoints now start blank for new projects and only
reload values that were actually saved in project JSON. The grid preview no
longer auto-loads a default-named grid CSV unless the saved project references
that grid file.

Bug fix: Audio Setup FS selector now shows the full text, including the
"(recommended)" suffix.

Files touched:
src/harmonic_drive_qt/audio_setup_pane.py
src/harmonic_drive_qt/grid_pane.py
src/harmonic_drive_qt/main.py
src/harmonic_drive_qt/main_window.py
tests/test_project.py
tests/test_qt_audio_setup_pane.py
tests/test_qt_grid_pane.py
tests/test_qt_main_window_project.py

--------
14/06/26
--------

Styled scrollbars with visible blue handles and widened the main
splitter divider so the center pane boundary is easier to grab.

Added SPL Calibration help tooltip to Audio Setup pane.

Moved the FR smoothing selector into the Frequency Response plot header.

Fixed plot lines drawing over the axis boundaries.

Changed the Impulse Response plot to show the linear waveform and use mirrored
dBFS labels.

Added a debug serial comms toggle for GRBL startup diagnostics. When enabled,
startup raises logging verbosity and records GRBL serial open/probe/ACK events.

--------
14/06/26
--------
Added 3D viewer resilience for machines where PyVista/VTK cannot start or render.
Grid Generator and Live Capture 3D views now fall back to the Matplotlib 3D
viewer and show a popup explaining that PyVista visualisation failed.

Moved the native Qt Audio Setup output level and FS controls onto the same top
line as the Audio API selector.


Files touched:
src/harmonic_drive_qt/grid_pane.py
src/harmonic_drive_qt/live_capture.py
src/grid/coord_viewer_core.py
src/harmonic_drive_qt/audio_setup_pane.py
src/harmonic_drive_qt/styles.py
src/harmonic_drive_qt/main_window.py
src/harmonic_drive_qt/widgets.py
src/harmonic_drive_qt/settings_dialog.py
src/harmonic_drive/config_editor.py
src/nfs/grbl_controller.py
src/nfs/logging_config.py
config_default.ini

--------
13/06/26
--------
Expanded the native Qt Settings pane motion-manager tab. The tab now exposes
 the motion-manager type and dynamic type-specific fields such as safe_radius.

Fixed native Qt Audio Setup combo styling. The Audio API and FS dropdowns now
render as normal framed fields sized to their contents, while the tinted
device/channel controls remain visually distinct. Dropdown popup font warnings
were resolved by using point-sized fonts for the shared Qt stylesheet.

Files touched:
src/harmonic_drive_qt/settings_dialog.py
src/harmonic_drive_qt/audio_setup_pane.py
src/harmonic_drive_qt/styles.py
tests/test_qt_settings_dialog.py
tests/test_qt_audio_setup_pane.py

--------
12/06/26
--------
Added a native Qt UI under src/harmonic_drive_qt built with PySide6. The new UI
reuses the existing scanner, audio, project, grid generation, and measurement
backend code while providing native Machine Control, Audio Setup, Live Capture,
Grid Generator, and Settings panes.

Added a PyVista 3D coordinate viewer for the native UI. Grid preview and
live measurement progress can use the PyVista viewer for interactive 3D
visualisation.

Files touched:
src/harmonic_drive_qt/
src/grid/coord_viewer_core_pyvista.py
pyproject.toml

--------
07/06/26
--------
Prepared a lower-pressure Tom test build. Measurement progress remains on the
backend-polled path, the in-app browser System Log viewer is disabled by
default, and scanner/DRO UI updates are coalesced onto the NiceGUI UI loop so
rapid hardware callbacks redraw the latest state at a controlled rate instead
of queuing one UI update per callback.

Kept lightweight reconnect diagnostics enabled for page/reload investigation,
with deeper scanner/progress UI pressure counters available behind the
[debug] deep_ui_diagnostics toggle. The plain Mock GRBL controller is quiet
again; active DRO/status callback simulation remains isolated to
MockSimulatedDRO.

Files touched:
src/harmonic_drive/control.py
src/harmonic_drive/reconnect_debug.py
src/harmonic_drive/config_editor.py
src/nfs/grbl_controller.py
config.ini

--------
06/06/26
--------
Made long-running measurement/audio notifications robust against browser
refreshes and reconnects. Completion toasts now use the client captured when
the action starts; if that client has gone away, the toast is skipped and the
backend work continues without raising a deleted parent-slot error.

Added small guards around post-await UI updates for measurement buttons, sine
icons, and safe-move button re-enabling so stale UI elements do not interrupt
measurement, audio, or motion completion paths.

Project change callbacks that belong to deleted browser clients are now removed
from the global callback list after their first stale-client failure, preventing
old rebuilt pages from repeatedly producing deleted-client warnings.

Audio meters now expire stale active readings if audio callbacks stop arriving,
so the SPL and Live Capture meters do not remain active after a sine stream is
stopped unexpectedly or the browser reconnects during tone generation.

Added temporary reconnect diagnostics for random splash/page rebuilds during
measurement runs. The logs now report app run ID, NiceGUI client connect and
disconnect events, page creation, browser lifecycle events, UI event-loop lag,
audio worker start/finish, scanner callback activity, and measurement progress
activity so browser reloads can be separated from server restarts and blocked UI
loops.

Added a separate MockSimulatedDRO GRBL controller option for local debugging.
Unlike the existing instant Mock controller, it simulates movement over time and
emits regular DRO/status callbacks from a background thread so real-hardware UI
update pressure can be reproduced without a physical controller.

Added a temporary [debug] direct_progress_ui config toggle to route measurement
progress updates through the old direct worker-callback UI path. The default
remains the safer backend-polling progress path.

Removed the temporary NiceGUI timer identity hook from normal app startup.

Added a measurement progress counter in the Machine Control pane. The bar
shows percent complete, measured points versus total points, and a backend
estimated time remaining that starts after the first two completed points and
updates on each point after that. Progress state is stored by the measurement
backend so the display can recover after a browser refresh.

Files touched:
src/harmonic_drive/control.py
src/harmonic_drive/gui.py
src/harmonic_drive/reconnect_debug.py
src/harmonic_drive/config_editor.py
src/harmonic_drive/live_capture.py
src/harmonic_drive/project.py
src/nfs/nfs.py
src/nfs/audio.py
src/nfs/grbl_controller.py
tests/test_grbl_controller.py
tests/test_near_field_scanner.py
config.ini

--------
05/06/26
--------
Fixed GRBL timeout handling. The short timeout is now only used for the initial
controller-present probe, rather than being applied to all transmitted GRBL
requests. Normal commands now wait indefinitely for their ack again, preserving
move and homing synchronization behavior.

Files touched:
src/nfs/grbl_controller.py
tests/test_grbl_controller.py

--------
04/06/26
--------
Added temporary NiceGUI timer identity logging so deleted-parent-slot errors
report the timer callback, interval, and creation stack.

Fixed the Generate Grid timer failure. Generate Grid can activate the
project folder, which rebuilds the Audio Setup panel and recreates the SPL
calibration meter timer. That timer now uses a module-owned background timer,
avoiding the deleted parent slot error.

Simplified the startup splash to remove the dot animation timer. The app loader
now uses an optional status callback instead of requiring a splash label object.

Files touched:
src/harmonic_drive/gui.py
src/harmonic_drive/control.py
src/harmonic_drive/audio_setup.py
src/harmonic_drive/timer_debug.py

--------
02/06/26
--------
Added scanner calibration tool height. The WCS zero command can now account for
a fixed calibration tool height so the true Z zero is below the current machine
position by the configured calibration tool height.

Updated tests.

Files touched:
src/nfs/scanner.py
src/harmonic_drive/config_editor.py
tests/test_scanner.py
tests/test_config_editor.py
tests/test_control.py
config.ini


--------
02/06/26
--------
Swapped Phi + and - symbols for CCW and CW labels.

--------
02/06/26
--------
Updated  Machine Control and Audio Setup.

Machine Control:

Now uses a single DRO frame above the motion controls.
An optional MCS row can be shown alongside the normal WCS row, with a toggle
to enable / dissable in app settings.

Updated the machine motion and command controls. Two motion-control layouts can
be toggled in Settings: the default layout gives direct per-axis step-size
access for Phi, Radius, and Height, while the alternative layout uses simpler
direction buttons with a global step-size selection.

Measurement-set controls now use one button for Start, Pause,
and Resume, plus a separate Stop button. Pause waits at clean measurement point
boundaries before the next position or sweep, while Stop cancels further points
and returns through the existing safe scanner return sequence.

The control pane layout is configurable from Settings without restarting the
app. ReHome and hight offset controls are hidden by default. Shutdown Program
now lives in the left menu under Settings.

Scanner connection failures no longer prevent unrelated app features from
loading. Missing or invalid COM ports now produce a clear warning
but let the app run. A short GRBL response timeout avoids
hanging on incorrect but valid COM ports.

Audio Setup now has an explicit microphone SPL calibration workflow: it shows a
held A-weighted mic reading, accepts a physical SPL meter reading, calculates and
saves the SPL offset only when Calibrate is pressed, and also allows manually
entering a known offset.

Live Capture audio meters are labelled from -60 dB to
0 dB, use RMS for the main bar, and show a slow-decay red peak marker. The
sine generator play button switches to a stop icon while the tone is playing.

Files touched:
src/harmonic_drive/control.py
src/harmonic_drive/gui.py
src/harmonic_drive/config_editor.py
src/harmonic_drive/audio_setup.py
src/harmonic_drive/live_capture.py
src/harmonic_drive/project.py
src/nfs/audio.py
src/nfs/grbl_controller.py
src/nfs/nfs.py
src/nfs/scanner.py
tests/test_grbl_controller.py
tests/test_near_field_scanner.py
tests/test_project.py
config.ini
default_config.ini

--------
30/05/26
--------
Added optional Grid Generator visualisation waypoints for post-processing:
Ref Origin plus the baffle bottom-left, top-left, and top-right corners. The
three baffle corner waypoints support visualisation of a sloped baffle plane.
The waypoint UI is now split into required measurement points and optional
visualisation points, with additional user markers still supported.

Generated grid CSV metadata now includes the reference-origin and baffle-corner
positions alongside the existing top, bottom, tweeter, and user-position
metadata. Added regression coverage for preserving dynamic user_position_*
entries in planned CSV output.

Files touched:
HALS_Projects/HALS_Test_Session/HALS_Test_Session_grid.csv
HALS_Projects/HALS_Test_Session/HALS_Test_Session_project.json
src/grid_generator/config_grid.py
src/grid_generator/grid_gen.py
src/grid_generator/grid_gen_gui.py
tests/test_grid_gen_metadata.py

--------
30/05/26
--------
Aligned the previously disparate names for extra user markers between the
project JSON and generated grid CSV. Project JSON now stores these entries as
user_positions to match the CSV user_position_* metadata naming. No backward
compatibility is provided for the old additional_positions key.

To make an older project JSON compatible, manually rename
"additional_positions" to "user_positions" in a text editor.

Files touched:
src/grid_generator/grid_gen_gui.py
tests/test_project.py

--------
30/05/26
--------
Bug fix: the Grid Generator 3D viewer now reloads an existing saved grid file
view after the active Session Folder has been resolved.

Files touched:
src/grid_generator/grid_gen_gui.py

--------
27/05/26
--------
Bug fix: removed the ambiguous no-session-selected state. On startup the Session
Folder field now shows "No session folder selected" and the default project
directory is only used as the file picker starting location. Save, grid
generation, single measurements, and full measurements now require the user to
choose or create a Session Folder first via a Browse/Cancel prompt. Test Sweep
remains in-memory and does not require a Session Folder.

Added tests for no-session/session-selected folder resolution and single
measurement output routing to the active Session Folder.

Files touched:
src/harmonic_drive/control.py
src/harmonic_drive/gui.py
src/harmonic_drive/project.py
src/grid_generator/grid_gen_gui.py
tests/test_project.py

--------
26/05/26
--------
Added controls in the config editor for GRBL streamer type,
baudrate, and COM port. COM ports are now listed from detected serial devices,
while preserving the configured port if it is not currently connected.

Added config editor tests for GRBL connection saving and serial port option
generation.

Files touched:
src/harmonic_drive/config_editor.py
tests/test_config_editor.py

--------
24/05/26
--------
Moved output level control from 'Sine Tone' section to the main audio settings
setion to make it clear this is a global control.

Files touched:
src/harmonic_drive/audio_setup.py


--------
24/05/26
--------
Bug fix: SPL calibration now stores the calculated offset from the measured mic
input RMS to the entered SPL value, instead of only saving the entered value.

Files touched:
src/harmonic_drive/audio_setup.py
src/harmonic_drive/project.py
tests/test_project.py

--------
24/05/26
--------
Added session-folder workflow to the HarmonicDrive UI. The top bar now lets the
user choose a Session Folder with the native folder picker, set a Save Name, and
save session state to <SaveName>_project.json. If no folder has been chosen, the
UI defaults to <default_project_dir>\HALS_Project, but only creates that folder
when the user saves, generates a grid, or starts a session.

Session files now store sweep setup data, SPL calibration if present, plus the
Grid Generator settings and waypoints needed to reopen or regenerate a grid.

Generated grids are saved into the active Session Folder as <SaveName>_grid.csv.

Full measurement output is routed to Session Folder\measurement_set,
Single measurements are routed separately to Session Folder\single_measurements.
The measurement position log stays at Session Folder\measurement_positions.csv,
and Scanner.log is saved under Session Folder\logs.

The Settings button now lives in the left sidebar and opens the
config editor. The config editor includes a default project directory and a
Restore Defaults action that restores default_config.ini

Added a dedicated Audio Setup pane and moved user-facing audio/sweep controls out
of the general config editor. The config editor remains the backend responsible
for validating, writing, and reloading config.ini, while its visible Audio tab now
only exposes the debug/test audio mode.

Audio Setup now includes:
- API-filtered device selectors
- sine tone generator for level setting
- SPL calibration field
- Test Sweep button
- All audio settings and sweep settings

Audio device selection is now more robust when device IDs change.
Config saves selected input/output device names as well as their device
IDs, uses the saved names to detect changes, and prompts the
user before updating the device IDs.

Test Sweep uses the active audio/sweep settings, including protection HPF and
sweep level, but returns the processed IR to Live Capture in memory rather than
writing permanent WAV, IR, or debug files.

Added real-time audio meters to Live Capture. Live Capture opens by default when
Audio Setup is opened so levels and Test Sweep results can be viewed while
adjusting the setup.

Files touched:
src/harmonic_drive/gui.py
src/harmonic_drive/audio_setup.py
src/harmonic_drive/project.py
src/harmonic_drive/control.py
src/harmonic_drive/config_editor.py
src/harmonic_drive/live_capture.py
src/grid_generator/grid_gen_gui.py
src/nfs/audio.py
src/nfs/nfs.py
changelog.txt

--------
23/05/26
--------
Added optional user-position fields to the grid generator UI for marking extra
speaker reference points such as woofer, midrange or port locations. These
positions are saved into the generated CSV metadata as user_position_* entries.
Can be referenced in post-processing as a visual assist.

Files touched:
src/grid_generator/grid_gen.py
src/grid_generator/grid_gen_gui.py
src/grid_generator/config_grid.py
changelog.txt

--------
22/05/26
--------
Added a compact "Drag to reorder" hint to the Live Capture header so the panel
drag handles are discoverable without taking much screen space.

Files touched:
live_capture.py
changelog.txt

--------
22/05/26
--------
Fixed Live Capture 3D Progress so switching back after generating a grid reloads
the latest grid file automatically.

Files touched:
live_capture.py
changelog.txt

--------
21/05/26
--------
Fixed: image loading by serving images through a stable NiceGUI static route instead of relying on auto-generated local file URLs.

Files touched:
grid_gen_gui.py
gui.py
changelog.txt

--------
19/05/26
--------
Fixed generated-grid reloads in the Plotly 3D coordinate viewer so playback and
auto-rotate do not snap back to a stale camera after a new grid is generated.
Generated-grid updates now preserve the browser's current Plotly camera while
refreshing the trace data, instead of pushing the Python-side figure layout with
an old camera value.

Files touched:
src/grid_generator/coord_viewer_core.py
changelog.txt

--------
19/05/26
--------
Removed stale pyfar and matplotlib dependencies now that audio.py uses the
in-project sweep generation, alignment and FFT deconvolution pipeline. Added
soundfile as an explicit dependency because audio.py and Live Capture read/write
WAV files directly. Updated README audio descriptions to match the current
custom DSP backend.

Files touched:
pyproject.toml
uv.lock
README.md
changelog.txt

--------
19/05/26
--------
Refined Live Capture Frequency Response smoothing controls. The header now uses
common fractional-octave presets (Off, 1/3, 1/6, 1/12, 1/24, 1/48), with the
selected value saved by the Save as Default button. Optimized the smoothing
calculation so long impulse responses no longer block initial page loading.

Files touched:
src/harmonic_drive/live_capture.py
changelog.txt

--------
19/05/26
--------
Added a Live Capture guard for empty measurement_positions.csv files so the
Measurement Positions plot stays in its waiting state without NumPy loadtxt
warnings while a new measurement session has no logged positions yet.

Files touched:
src/harmonic_drive/live_capture.py
changelog.txt

--------
19/05/26
--------
Switched the Live Capture acoustic analysis plots from matplotlib to Plotly,
including the Measurement Positions, Impulse Response and Frequency Response
panels. Removed the direct matplotlib dependency from pyproject.toml; matplotlib
may still be installed transitively by pyfar.

Added fractional-octave smoothing to the Live Capture Frequency Response plot.
The smoothing amount is adjustable from the Live Capture header with the
FR Smooth 1/ control, and is saved to config.ini under [live_capture] with
frequency_smoothing_fraction = 24. A value of 24 means 1/24-octave smoothing,
and 0 disables smoothing.

Files touched:
src/harmonic_drive/live_capture.py
pyproject.toml
uv.lock
config.ini
changelog.txt

--------
19/05/26
--------
Removed redundant matplotlib usage from the Plotly-based grid viewer and grid
generator modules. coord_viewer_core.py now uses a small colorsys HSV-to-hex
helper for the color slider, and grid_gen.py no longer imports stale pyplot or
Axes3D dependencies.

Files touched:
src/grid_generator/coord_viewer_core.py
src/grid_generator/grid_gen.py
changelog.txt

--------
19/05/26
--------
Added user-draggable ordering for Live Capture panels, with persistent saved
panel order and visibility settings in config.ini. Saved/restored layouts now
also reorder the Live Capture visibility buttons so the buttons follow the same
order as the panels.

Changed the Frequency Response x-axis tick labels to standard audio-style
frequency labels such as 1k, 2k, 5k and 10k instead of scientific notation.

Files touched:
src/harmonic_drive/live_capture.py
changelog.txt

--------
19/05/26
--------
Improved Plotly 3D viewer rotate/play performance when bounds are visible.
The browser render loop now avoids full Plotly.react calls during normal
animation, using camera-only relayout updates for rotation and targeted restyle
updates for the snake, head and history traces.

Added a rotation speed setting to the More dropdown, in degrees per second,
with a default of 5 deg/s. Rotation timing now uses elapsed browser frame time
so the selected speed is independent of frame rate.

Files touched:
src/grid_generator/coord_viewer_core.py
src/grid_generator/grid_gen_gui.py
src/harmonic_drive/live_capture.py
changelog.txt

--------
19/05/26
--------
Added a 3D Measurement Progress viewer to Live Capture using
coord_viewer_core.py. It loads the full planned grid file and advances the
snake/current-point marker from the number of completed rows in the measurement
position log, rather than playing points back manually.

The Live Capture 3D viewer includes view/display controls such as Top,
Front, Side, Ortho, Bounds, Rotate and display settings.

Files touched:
src/harmonic_drive/live_capture.py
changelog.txt

--------
19/05/26
--------
Renamed the right-pane Acoustic Analysis view to Live Capture and moved the
code from acoustic_analysis.py into live_capture.py.

Moved the measurement positions azimuth-vs-elevation plot out of control.py and
into live_capture.py, so the left pane stays focused on user controls and live
position readouts.

Split the old combined acoustic plot into separate Frequency Response and
Impulse Response plots. Added lozenge buttons on the Live Capture title row to
choose which plots are shown:

Files touched:
src/harmonic_drive/live_capture.py
src/harmonic_drive/gui.py
src/harmonic_drive/control.py
src/harmonic_drive/acoustic_analysis.py

--------
19/05/26
--------
Made the grid generator file auto-load into the measurement session after it is
generated. Added set_file_measurement_points_filename() to config_editor.py to
update the FileMeasurementPoints filename in config.ini and trigger a reload.
Added an on_grid_saved_callback to grid_gen_gui.py to pass the generated filename
back to the main Harmonic Drive app.

Files touched:
src/grid_generator/grid_gen_gui.py
src/harmonic_drive/gui.py
src/harmonic_drive/control.py
src/harmonic_drive/config_editor.py
tests/test_config_editor.py
config.ini

--------
19/05/26
--------
Renamed the control.py position readout from Theta to Phi so the GUI matches
the phi_deg naming used by generated measurement files. Kept the internal
CylindricalPosition t() accessor unchanged because it is still the existing
angular coordinate API used by the scanner and motion code.

Files touched:
src/harmonic_drive/control.py

--------
19/05/26
--------
Split the main Harmonic Drive GUI into smaller modules so gui.py is mostly
layout and navigation. Added a left side bar menu to select what is displayed
in the right hand pane.

Moved the main scanner controls into control.py. This is always visible in the
left hand pane.

Moved the acoustic analysis into acoustic_analysis.py.

Added grid_generator directory with grid_gen.py, path_plan.py, coord_viewer_core.py
and a NiceGUI UI.

The right pane mview can switch with the menu between Acoustic Analysis and
the Grid Generator (and future features).

Connected grid_gen_gui.py waypoint Set-position buttons to the live scanner position
via a callback.

Other changes needed:
- Added grid_generator as a package with __init__.py.
- Added pandas and plotly to pyproject.toml and regenerated uv.lock.

Files touched:
src/harmonic_drive/gui.py
src/harmonic_drive/control.py
src/harmonic_drive/acoustic_analysis.py
src/grid_generator/grid_gen_gui.py
src/grid_generator/__init__.py
pyproject.toml
uv.lock

--------
19/05/26
--------
Made homing_gap, pole_gap and safe_radius optional, defaulting to. These settings are
mostly redundant for grids generated by the new d.fapinov built grid
generator, since path planning is handled up front, but they remain
available as extra safety margins when needed.

Files touched:
- src/nfs/plugins/file_measurement_points.py
- src/nfs/motion_manager.py
- src/harmonic_drive/config_editor.py
- tests/test_config_editor.py
- tests/test_optional_motion_and_file_points.py

file_measurement_points.py
---------------------------------

27/02/26
Bug in def ready:
>= stops when current position count is equal to the total so does nothing on the final position. Changed to > only.

27/02/26
Bug in def _remove_point_inside_homing_area:
& operator needs current angle to be both greater than +175 and less than -175 at the same time so always returns False. Changed to abs() function so the sign does not matter.

audio.py
--------------------------------

27/02/26
Refactor to follow Single Responsibility Principle (SRP).

RunSweep - Conductor that manages audio streams and execution of the other audio processing classes.

DSPUtils - A utility class containing pure mathematical functions such as Hann windowing, dB-to-linear conversion, and cross-correlation.

MarkerGenerator - Responsible for creating the Barker-13 sequence used to synchronize the timing of the recordings.

SweepGenerator - Handles the creation of the sweep signal and its corresponding inverse filter.

HarmonicInjector - A testing class that adds H2 or H3 harmonic distortion to the sweep signal to verify Farina distortion separation capability.

ProtectionFilter - Applies a high-pass filter (either Minimum or Linear Phase) to the playback signal to protect speaker drivers from damage.

AlignmentEngine - Manages temporal alignment of multiple sweeps by detecting the Barker markers and performs averaging to reduce noise.

DeconvolutionEngine - The core engine that performs deconvolution, applies spectral masking (DC and Nyquist minimum-phase filters), and separates the linear response from distortion.

27/02/26
Bug in audio_worker finally block:
Thread crash on full measurement set completion. Attempted to access non-existent app.loop attribute. Changed to capture the active event loop with asyncio.get_running_loop() in the UI thread and passing it to the worker.

28/02/26
Added MockInterfaceAudio class, enabled by config.ini. Simulates an audio interface so the sweep and IR generation pipeline can be verified without hardware. Simulation includes - Driver latency of 20ms, FIR anti-aliasing LPF based on CS4272 DAC specs, FIR filter group delay of 12/FS, DC blocking HPF based on C 10uF / R 10K ~15Hz 1st order.

28/02/26
Added a block in _matched_filter_detect to calculate the peak to sidelobe ratio of the barker marker correlation and flag a logger.warning() when the value is below 2.5 indicating potential alignment issues.

28/02/26
Added alignment_debug.npz export when debug_saves is enabled. Includes raw loopback, reference Barker-13, cross-correlation results, and quality metrics Peak-to-Sidelobe Ratio and match percent. Enables analysis of barker marker alignment quality using util analyze_marker_alignment.py. Implemented originally to inspect blurring caused by hardware DC blocking capacitors on audio interfaces.

28/02/26
Added user variable for wav file naming convention. Tom for method compatible with Tom's processing code. Dimitri for the method compatible with Dimitri's processing code.
